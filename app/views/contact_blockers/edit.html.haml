.edit
	= render partial: 'shared/nav_title', locals: { title: t('views.contact_blocker.edit.title', email: @contact_blocker.email) }
	%p#notice= notice
	
	#forms
		= render partial: 'form'
		%br
		= link_to 'Show', @contact_blocker
		|
		= link_to 'List', contact_blockers_path
		|
		= link_to 'Remove', @contact_blocker, method: :delete, data: { confirm: t('views.contact_blocker.edit.confirm_destroy', email: @contact_blocker.email) }

= render partial: 'shared/save_changes_prompt'
