- @styles = @styles.nil? || @styles
!!! XML
!!!
%html
	%head
		%title= @page_title.presence || "#{company_name} | #{company_tagline}"
		- if @styles
			= stylesheet_link_tag "interior", media: "all"
		= csrf_meta_tags
	%body
		= yield
		= render partial: 'layouts/tracking'
		= render partial: 'layouts/g_analytics'
