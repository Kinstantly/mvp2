New review for #{@review.profile.display_name_or_company} by #{@review.reviewer_username}:

- if @review.title.present?
	= label :review, :title
	= @review.title
	
- if @review.body.present?
	= label :review, :body
	= @review.body
	
- if @review.good_to_know.present?
	= label :review, :good_to_know
	= @review.good_to_know

Edit/Delete review:
= edit_plain_profile_url(@review.profile) + "#edit_review_div_#{@review.id}"
