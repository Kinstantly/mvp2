%h3= Profile.human_attribute_name(:certifications)
.edit.popover
	= form_for @profile, url: formlet_update_profile_path(@profile), remote: true, html: {id: "edit-#{formlet}"} do |f|
		= f.error_messages
		= hidden_field_tag 'formlet', formlet
		.text_field#certifications_field
			%span.char_count
			= f.label :certifications
			= f.text_field :certifications, class: 'attr'
			%p.note= t 'views.profile.edit.certifications_example'
		= f.submit 'Save', disable_with: 'Saving...', class: 'save'
		= link_to 'Cancel', '#', class: 'cancel button'
%p.attribute_display= profile_display_text @profile.errorfree.certifications, serialize: true, links: true

:coffeescript
	# Show character count while filling in field.
	$('#certifications_field input').simplyCountable({counter: '#certifications_field .char_count', maxCount: my_vars.edit_profile.max_string_length})
